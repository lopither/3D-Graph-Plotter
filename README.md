# 📘 3D Function Plotter in Wolfram Language

This interactive Wolfram Language program allows users to input a custom mathematical function of two variables (`x` and `y`) along with custom bounds, and plots the result in 3D.

---

## 🚀 Features

- Interactive command-line style input using `InputString`
- Supports any well-formed function using Wolfram Language syntax
- Validates function input (ensures it includes both `x` and `y`)
- Handles advanced functions like `Sin[x + y]/Sqrt[Exp[x - y]]`
- Customizable axis bounds
- Displays a full 3D surface plot with labeled axes

---

## 🛠️ Requirements

- Wolfram Desktop (Mathematica) or Wolfram Engine

---

## 🧠 How to Use

Run the code in a Wolfram Notebook or Wolfram Script session.

You will be prompted to enter:
1. A function of `x` and `y`, e.g.: `Sin[x + y]/Sqrt[Exp[x - y]]`
2. Bounds for `x` and `y`, e.g.: `-5, 5, -5, 5`
   
